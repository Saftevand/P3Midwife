<Window x:Class="P3_Midwife.Views.NewChildWindow"
        x:Name="NCView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ViewModel="clr-namespace:P3_Midwife.ViewModel"
        xmlns:local="clr-namespace:P3_Midwife.Views"
        mc:Ignorable="d"
        Title="NewChildWindow" Height="720" Width="1280">

    <Window.DataContext>
        <ViewModel:NewChildViewModel/>
    </Window.DataContext>
    
    <Grid>
        <Menu Width="50" Height="20" VerticalAlignment="Top" HorizontalAlignment="Left">
            <MenuItem Header="Menu">
                <MenuItem Command="{Binding BackCommand}" Header="Tilbage"/>
                <MenuItem Command="{Binding LogOutCommand}" Header="Log ud"/>
                <MenuItem Command="{Binding ExitCommand}" Header="Exit"/>
            </MenuItem>
        </Menu>

        <ListView x:Name="listView" HorizontalAlignment="Left" Height="654" Margin="0,20,0,0" VerticalAlignment="Top" Width="200">
            <ListViewItem Content="{Binding CurrentPatient.Name}" Margin="0"/>
            <ListViewItem Content="{Binding CurrentPatient.Age}" Margin="0"/>
            <ListViewItem Content="{Binding CurrentPatient.Gender}" Margin="0"/>
            <ListViewItem Content="{Binding CurrentPatient.CPR}" Margin="0"/>
        </ListView>
        
        
    </Grid>
</Window>
